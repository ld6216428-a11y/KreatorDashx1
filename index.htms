<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>KreatorDash — Creator Workspace</title>P        <div class="card">
          <div class="chart-wrap">
            <canvas id="overviewChart"></canvas>
          </div>
        </div>

        <div class="grid two">
          <div>
            <div class="section-title">
              <h3>Recent Expenses</h3>
              <span id="expenseCount">0 items</span>
            </div>
            <div class="card" id="dashboardExpenses"></div>
          </div>

          <div>
            <div class="section-title">
              <h3>Upcoming Content</h3>
              <span id="scheduleCount">0 items</span>
            </div>
            <div class="card" id="dashboardSchedules"></div>
          </div>
        </div>

        <div class="notice">
              Semua angka revenue di KreatorDash adalah estimasi, bukan pendapatan resmi dari platform.
              Hasil sebenarnya dapat berbeda tergantung negara audiens, niche, monetisasi, kontrak brand,
              jenis konten, dan kebijakan platform.
        </div>
      </section>

      <!-- REVENUE -->
      <section class="page" id="revenue">
        <div class="card">
          <div class="section-title">
            <h3>Revenue Calculator</h3>
            <span>Estimasi pendapatan creator</span>
          </div>

          <div class="form-grid">
            <div class="field">
              <label>Platform</label>
              <select id="platform">
                <option>TikTok</option>
                <option>Instagram</option>
                <option>Facebook</option>
                <option>YouTube Shorts</option>
              </select>
            </div>

            <div class="field">
              <label>Views per month</label>
              <input id="views" type="number" min="0" placeholder="Contoh: 100000" />
            </div>

            <div class="field">
              <label>Followers</label>
              <input id="followers" type="number" min="0" placeholder="Contoh: 25000" />
            </div>

            <div class="field">
              <label>RPM per 1.000 views — USD</label>
              <input id="rpm" type="number" min="0" step="0.01" value="0.50" />
            </div>

            <div class="field">
              <label>Likes per month</label>
              <input id="likes" type="number" min="0" placeholder="Contoh: 5000" />
            </div>

            <div class="field">
              <label>Comments per month</label>
              <input id="comments" type="number" min="0" placeholder="Contoh: 500" />
            </div>

            <div class="field">
              <label>Shares per month</label>
              <input id="shares" type="number" min="0" placeholder="Contoh: 800" />
            </div>
          </div>

          <div class="form-actions">
            <button class="btn primary" onclick="calculateRevenue()">Calculate</button>
            <button class="btn" onclick="clearRevenueForm()">Clear</button>
          </div>

          <div class="result-box">
            <div class="card-label">Estimated Monthly Revenue</div>
            <strong id="monthlyResult">$0</strong>
            <div class="card-small" id="monthlyIdr">Rp0</div>

            <div style="margin-top:20px" class="card-label">Estimated Yearly Revenue</div>
            <strong id="yearlyResult">$0</strong>
            <div class="card-small" id="yearlyIdr">Rp0</div>

            <div style="margin-top:20px" class="card-label">Engagement Rate</div>
            <strong id="engagementResult">0%</strong>
          </div>
        </div>
      </section>

      <!-- BUDGET -->
      <section class="page" id="budget">
        <div class="grid stats">
          <div class="card">
            <div class="card-label">Total Revenue</div>
            <div class="card-value" id="budgetRevenue">$0</div>
          </div>

          <div class="card">
            <div class="card-label">Total Expenses</div>
            <div class="card-value" id="budgetExpenses">$0</div>
          </div>

          <div class="card">
            <div class="card-label">Net Profit</div>
            <div class="card-value" id="budgetProfit">$0</div>
          </div>

          <div class="card">
            <div class="card-label">Expense Items</div>
            <div class="card-value" id="budgetCount">0</div>
          </div>
        </div>

        <div class="section-title">
          <h3>Add Expense</h3>
        </div>

        <div class="card">
          <div class="form-grid">
            <div class="field">
              <label>Expense name</label>
              <input id="expenseName" placeholder="Contoh: Kamera, lighting, editing" />
            </div>

            <div class="field">
              <label>Amount — USD</label>
              <input id="expenseAmount" type="number" min="0" step="0.01" placeholder="Contoh: 50" />
            </div>

            <div class="field">
              <label>Category</label>
              <select id="expenseCategory">
                <option>Equipment</option>
                <option>Production</option>
                <option>Editing</option>
                <option>Marketing</option>
                <option>Transport</option>
                <option>Other</option>
              </select>
            </div>
          </div>

          <div class="form-actions">
            <button class="btn primary" onclick="addExpense()">Add Expense</button>
            <button class="btn" onclick="exportExpenses()">Export CSV</button>
          </div>
        </div>

        <div class="section-title">
          <h3>Expense History</h3>
        </div>

        <div class="card" id="expenseList"></div>
      </section>

      <!-- RATE CARD -->
      <section class="page" id="ratecard">
        <div class="card">
          <div class="section-title">
            <h3>Rate Card Builder</h3>
            <span>Estimasi harga kerja sama</span>
          </div>

          <div class="form-grid">
            <div class="field">
              <label>Average views</label>
              <input id="rateViews" type="number" min="0" placeholder="Contoh: 50000" />
            </div>

            <div class="field">
              <label>Followers</label>
              <input id="rateFollowers" type="number" min="0" placeholder="Contoh: 10000" />
            </div>

            <div class="field">
              <label>Engagement rate (%)</label>
              <input id="rateEngagement" type="number" min="0" step="0.1" placeholder="Contoh: 5" />
            </div>

            <div class="field">
              <label>Content type</label>
              <select id="contentType">
                <option>Short Video</option>
                <option>Story</option>
                <option>Feed Post</option>
                <option>Live Session</option>
                <option>UGC Video</option>
              </select>
            </div>

            <div class="field">
              <label>Extra revisions</label>
              <input id="revisions" type="number" min="0" value="0" />
            </div>

            <div class="field">
              <label>Usage rights</label>
              <select id="usageRights">
                <option>Organic Only</option>
                <option>Brand Repost</option>
                <option>Paid Ads</option>
                <option>Full Commercial Rights</option>
              </select>
            </div>

            <div class="field">
              <label>Exclusivity</label>
              <select id="exclusivity">
                <option>None</option>
                <option>1 Month</option>
                <option>3 Months</option>
                <option>6 Months</option>
              </select>
            </div>
          </div>

          <div class="form-actions">
            <button class="btn primary" onclick="calculateRateCard()">Generate Rate</button>
          </div>

          <div class="result-box">
            <div class="card-label">Suggested Starting Rate</div>
            <strong id="rateResult">$0</strong>
            <div class="card-small" id="rateExplanation">Isi data untuk menghitung rate.</div>
          </div>
        </div>
      </section>

      <!-- STUDIO -->
      <section class="page" id="studio">
        <div class="grid two">
          <div class="card">
            <div class="section-title">
              <h3>Content Idea Generator</h3>
            </div>

            <div class="field">
              <label>Niche</label>
              <input id="ideaNiche" placeholder="Contoh: beauty, fashion, lifestyle" />
            </div>

            <div class="field" style="margin-top:15px">
              <label>Content goal</label>
              <select id="ideaGoal">
                <option>Increase Engagement</option>
                <option>Increase Followers</option>
                <option>Promote Product</option>
                <option>Build Personal Brand</option>
                <option>Educate Audience</option>
              </select>
            </div>

            <div class="form-actions">
              <button class="btn primary" onclick="generateIdea()">Generate Idea</button>
            </div>

            <div id="ideaResult" class="result-box" style="display:none"></div>
          </div>

          <div class="card">
            <div class="section-title">
              <h3>Script Editor</h3>
              <span>Save locally</span>
            </div>

            <div class="field">
              <label>Script title</label>
              <input id="scriptTitle" placeholder="Contoh: 3 kesalahan skincare pemula" />
            </div>

            <div class="field" style="margin-top:15px">
              <label>Script</label>
              <textarea id="scriptText" placeholder="Tulis hook, isi, dan CTA di sini..."></textarea>
            </div>

            <div class="form-actions">
              <button class="btn primary" onclick="saveScript()">Save Script</button>
            </div>
          </div>
        </div>

        <div class="section-title">
          <h3>Saved Scripts</h3>
        </div>

        <div class="card" id="scriptList"></div>
      </section>

      <!-- CALENDAR -->
      <section class="page" id="calendar">
        <div class="card">
          <div class="section-title">
            <h3>Content Calendar</h3>
            <span id="calendarMonth"></span>
          </div>

          <div class="form-grid">
            <div class="field">
              <label>Content title</label>
              <input id="scheduleTitle" placeholder="Contoh: Upload review parfum" />
            </div>

            <div class="field">
              <label>Date</label>
              <input id="scheduleDate" type="date" />
            </div>

            <div class="field">
              <label>Platform</label>
              <select id="schedulePlatform">
                <option>TikTok</option>
                <option>Instagram</option>
                <option>YouTube</option>
                <option>Facebook</option>
              </select>
            </div>

            <div class="field">
              <label>Status</label>
              <select id="scheduleStatus">
                <option>Planned</option>
                <option>In Progress</option>
                <option>Published</option>
              </select>
            </div>
          </div>

          <div class="form-actions">
            <button class="btn primary" onclick="saveSchedule()">Save Schedule</button>
            <button class="btn" onclick="clearScheduleForm()">Clear</button>
          </div>

          <div class="calendar-scroll">
            <div id="calendarGrid" class="calendar"></div>
          </div>
        </div>

        <div class="section-title">
          <h3>All Scheduled Content</h3>
        </div>

        <div class="card" id="scheduleList"></div>
      </section>

      <!-- SETTINGS -->
      <section class="page" id="settings">
        <div class="card">
          <div class="section-title">
            <h3>Settings</h3>
          </div>

          <div class="form-grid">
            <div class="field">
              <label>Creator name</label>
              <input id="creatorName" placeholder="Nama kamu" />
            </div>

            <div class="field">
              <label>Currency display</label>
              <select id="displayCurrency">
                <option>USD</option>
                <option>IDR</option>
              </select>
            </div>
          </div>

          <div class="form-actions">
            <button class="btn primary" onclick="saveSettings()">Save Settings</button>
            <button class="btn" onclick="exportAllData()">Export All Data</button>
            <button class="btn danger" onclick="clearAllData()">Clear All Data</button>
          </div>
        </div>

        <div class="notice">
          Data KreatorDash saat ini disimpan di localStorage browser. Jika cache browser dihapus
          atau kamu membuka website dari perangkat lain, data tidak otomatis ikut berpindah.
        </div>
      </section>

      <footer>
        KreatorDash © 2026 — Built for creators.
      </footer>
    </main>
  </div>

  <script>
    const state = {
      revenue: JSON.parse(localStorage.getItem("kd_revenue") || "{}"),
      expenses: JSON.parse(localStorage.getItem("kd_expenses") || "[]"),
      scripts: JSON.parse(localStorage.getItem("kd_scripts") || "[]"),
      schedules: JSON.parse(localStorage.getItem("kd_schedules") || "[]"),
      settings: JSON.parse(localStorage.getItem("kd_settings") || '{"name":"Creator","currency":"USD"}'),
      exchangeRate: 16000,
      editingScheduleId: null
    };

    let overviewChart = null;

    const $ = id => document.getElementById(id);

    function money(value, currency = "USD") {
      const number = Number(value) || 0;

      if (currency === "IDR") {
        return new Intl.NumberFormat("id-ID", {
          style: "currency",
          currency: "IDR",
          maximumFractionDigits: 0
        }).format(number);
      }

      return new Intl.NumberFormat("en-US", {
        style: "currency",
        currency: "USD",
        maximumFractionDigits: 2
      }).format(number);
    }
    
    return new Intl.NumberFormat("en-EU", {
        style: "currency",
        currency: "EUR",
        maximumFractionDigits: 2
      }).format(number);
    }

    function saveState() {
      localStorage.setItem("kd_revenue", JSON.stringify(state.revenue));
      localStorage.setItem("kd_expenses", JSON.stringify(state.expenses));
      localStorage.setItem("kd_scripts", JSON.stringify(state.scripts));
      localStorage.setItem("kd_schedules", JSON.stringify(state.schedules));
      localStorage.setItem("kd_settings", JSON.stringify(state.settings));
    }

    function showPage(page) {
      document.querySelectorAll(".page").forEach(item => {
        item.classList.remove("active");
      });

      document.querySelectorAll(".nav button").forEach(item => {
        item.classList.remove("active");
      });

      $(page).classList.add("active");

      const button = document.querySelector(`[data-page="${page}"]`);
      if (button) button.classList.add("active");

      const titles = {
        dashboard: ["Good day, Creator.", "Kelola performa, konten, dan pendapatanmu dalam satu tempat."],
        revenue: ["Revenue Calculator", "Hitung estimasi pendapatan berdasarkan performa konten."],
        budget: ["Budget Tracker", "Pantau biaya produksi dan keuntungan bersih."],
        ratecard: ["Rate Card Builder", "Buat estimasi harga kerja sama brand."],
        studio: ["Content Studio", "Cari ide, susun hook, dan simpan script konten."],
        calendar: ["Content Calendar", "Atur jadwal konten supaya lebih konsisten."],
        settings: ["Settings", "Atur identitas dan data workspace kamu."]
      };

      $("pageTitle").textContent = titles[page][0];
      $("pageSubtitle").textContent = titles[page][1];

      if (window.innerWidth <= 760) {
        $("sidebar").classList.remove("open");
      }

      if (page === "dashboard") renderDashboard();
      if (page === "budget") renderBudget();
      if (page === "studio") renderScripts();
      if (page === "calendar") renderCalendar();
    }

    function toggleSidebar() {
      $("sidebar").classList.toggle("open");
    }

    document.querySelectorAll(".nav button").forEach(button => {
      button.addEventListener("click", () => showPage(button.dataset.page));
    });

    async function loadExchangeRate() {
      try {
        const response = await fetch("https://open.er-api.com/v6/latest/USD");
        const data = await response.json();

        if (data && data.rates && data.rates.IDR) {
          state.exchangeRate = data.rates.IDR;
          $("rateStatus").textContent = `1 USD ≈ Rp${Math.round(state.exchangeRate).toLocaleString("id-ID")}`;
        }
      } catch (error) {
        $("rateStatus").textContent = "Kurs menggunakan nilai cadangan.";
      }
    }

    function calculateRevenue() {
      const views = Math.max(0, Number($("views").value) || 0);
      const followers = Math.max(0, Number($("followers").value) || 0);
      const rpm = Math.max(0, Number($("rpm").value) || 0);
      const likes = Math.max(0, Number($("likes").value) || 0);
      const comments = Math.max(0, Number($("comments").value) || 0);
      const shares = Math.max(0, Number($("shares").value) || 0);

      const monthlyUsd = views / 1000 * rpm;
      const yearlyUsd = monthlyUsd * 12;
      const engagement = followers > 0
        ? ((likes + comments + shares) / followers) * 100
        : 0;

      state.revenue = {
        platform: $("platform").value,
        views,
        followers,
        rpm,
        likes,
        comments,
        shares,
        monthlyUsd,
        yearlyUsd,
        engagement
      };

      $("monthlyResult").textContent = money(monthlyUsd);
      $("monthlyIdr").textContent = money(monthlyUsd * state.exchangeRate, "IDR");
      $("yearlyResult").textContent = money(yearlyUsd);
      $("yearlyIdr").textContent = money(yearlyUsd * state.exchangeRate, "IDR");
      $("engagementResult").textContent = engagement.toFixed(2) + "%";

      saveState();
      renderDashboard();
    }

    function clearRevenueForm() {
      ["views", "followers", "likes", "comments", "shares"].forEach(id => $(id).value = "");
      $("rpm").value = "0.50";
      state.revenue = {};
      saveState();

      $("monthlyResult").textContent = "$0";
      $("monthlyIdr").textContent = "Rp0";
      $("yearlyResult").textContent = "$0";
      $("yearlyIdr").textContent = "Rp0";
      $("engagementResult").textContent = "0%";
    }

    function addExpense() {
      const name = $("expenseName").value.trim();
      const amount = Number($("expenseAmount").value) || 0;
      const category = $("expenseCategory").value;

      if (!name || amount <= 0) {
        alert("Isi nama pengeluaran dan nominal yang valid.");
        return;
      }

      state.expenses.unshift({
        id: Date.now(),
        name,
        amount,
        category,
        date: new Date().toISOString()
      });

      $("expenseName").value = "";
      $("expenseAmount").value = "";

      saveState();
      renderBudget();
      renderDashboard();
    }

    function deleteExpense(id) {
      if (!confirm("Hapus pengeluaran ini?")) return;

      state.expenses = state.expenses.filter(item => item.id !== id);
      saveState();
      renderBudget();
      renderDashboard();
    }

    function renderBudget() {
      const revenue = state.revenue.monthlyUsd || 0;
      const expenses = state.expenses.reduce((sum, item) => sum + item.amount, 0);
      const profit = revenue - expenses;

      $("budgetRevenue").textContent = money(revenue);
      $("budgetExpenses").textContent = money(expenses);
      $("budgetProfit").textContent = money(profit);
      $("budgetCount").textContent = state.expenses.length;

      if (!state.expenses.length) {
        $("expenseList").innerHTML = `<div class="empty">Belum ada pengeluaran.</div>`;
        return;
      }

      $("expenseList").innerHTML = `
        <div class="list">
          ${state.expenses.map(item => `
            <div class="list-item">
              <div>
                <strong>${escapeHtml(item.name)}</strong>
                <small>${escapeHtml(item.category)} · ${formatDate(item.date)}</small>
              </div>
              <div style="text-align:right">
                <strong class="negative">-${money(item.amount)}</strong>
                <button class="btn danger" style="margin-top:6px;padding:5px 8px;font-size:11px" onclick="deleteExpense(${item.id})">Delete</button>
              </div>
            </div>
          `).join("")}
        </div>
      `;
    }

    function calculateRateCard() {
      const views = Number($("rateViews").value) || 0;
      const followers = Number($("rateFollowers").value) || 0;
      const engagement = Number($("rateEngagement").value) || 0;
      const type = $("contentType").value;
      const revisions = Number($("revisions").value) || 0;
      const usage = $("usageRights").value;
      const exclusivity = $("exclusivity").value;

      let base = Math.max(50, views * 0.001 + followers * 0.002);
      base *= 1 + engagement / 100;

      const typeMultiplier = {
        "Short Video": 1.2,
        "Story": .5,
        "Feed Post": .8,
        "Live Session": 1.5,
        "UGC Video": 1.3
      };

      const usageMultiplier = {
        "Organic Only": 1,
        "Brand Repost": 1.2,
        "Paid Ads": 1.6,
        "Full Commercial Rights": 2
      };

      const exclusivityMultiplier = {
        "None": 1,
        "1 Month": 1.15,
        "3 Months": 1.35,
        "6 Months": 1.7
      };

      let result = base * typeMultiplier[type];
      result *= usageMultiplier[usage];
      result *= exclusivityMultiplier[exclusivity];
      result += revisions * 25;

      $("rateResult").textContent = money(result);
      $("rateExplanation").textContent =
        `Perhitungan mempertimbangkan views, followers, engagement, jenis konten, revisi, usage rights, dan exclusivity.`;
    }

    function generateIdea() {
      const niche = $("ideaNiche").value.trim() || "lifestyle";
      const goal = $("ideaGoal").value;

      const ideas = [
        `3 kesalahan paling umum dalam ${niche} yang masih sering dilakukan pemula`,
        `Aku mencoba cara viral ${niche} selama 7 hari, ini hasilnya`,
        `Hal yang tidak pernah diberitahu orang tentang ${niche}`,
        `Before-after: perubahan setelah konsisten melakukan ${niche}`,
        `5 tips ${niche} yang bisa kamu coba hari ini`,
        `Reaksi jujur terhadap tren terbaru di dunia ${niche}`
      ];

      const idea = ideas[Math.floor(Math.random() * ideas.length)];

      $("ideaResult").style.display = "block";
      $("ideaResult").innerHTML = `
        <div class="card-label">Generated Content Idea</div>
        <strong style="font-size:22px">${escapeHtml(idea)}</strong>
        <p style="margin-top:12px;color:var(--muted);font-size:13px">
          Goal: ${escapeHtml(goal)}
        </p>
        <div style="margin-top:15px">
          <span class="tag">Hook kuat</span>
          <span class="tag">Storytelling</span>
          <span class="tag">CTA</span>
        </div>
      `;
    }

    function saveScript() {
      const title = $("scriptTitle").value.trim();
      const text = $("scriptText").value.trim();

      if (!title || !text) {
        alert("Isi judul dan script terlebih dahulu.");
        return;
      }

      state.scripts.unshift({
        id: Date.now(),
        title,
        text,
        date: new Date().toISOString()
      });

      $("scriptTitle").value = "";
      $("scriptText").value = "";

      saveState();
      renderScripts();
    }

    function deleteScript(id) {
      if (!confirm("Hapus script ini?")) return;

      state.scripts = state.scripts.filter(item => item.id !== id);
      saveState();
      renderScripts();
    }

    function renderScripts() {
      if (!state.scripts.length) {
        $("scriptList").innerHTML = `<div class="empty">Belum ada script yang disimpan.</div>`;
        return;
      }

      $("scriptList").innerHTML = `
        <div class="list">
          ${state.scripts.map(item => `
            <div class="list-item">
              <div style="flex:1">
                <strong>${escapeHtml(item.title)}</strong>
                <small>${formatDate(item.date)}</small>
                <p style="margin-top:10px;color:var(--muted);font-size:13px;white-space:pre-wrap">${escapeHtml(item.text)}</p>
              </div>
              <button class="btn danger" onclick="deleteScript(${item.id})">Delete</button>
            </div>
          `).join("")}
        </div>
      `;
    }

    function saveSchedule() {
      const title = $("scheduleTitle").value.trim();
      const date = $("scheduleDate").value;
      const platform = $("schedulePlatform").value;
      const status = $("scheduleStatus").value;

      if (!title || !date) {
        alert("Isi judul dan tanggal konten.");
        return;
      }

      if (state.editingScheduleId) {
        const item = state.schedules.find(item => item.id === state.editingScheduleId);

        if (item) {
          item.title = title;
          item.date = date;
          item.platform = platform;
          item.status = status;
        }

        state.editingScheduleId = null;
      } else {
        state.schedules.push({
          id: Date.now(),
          title,
          date,
          platform,
          status
        });
      }

      clearScheduleForm();
      saveState();
      renderCalendar();
      renderDashboard();
    }

    function editSchedule(id) {
      const item = state.schedules.find(item => item.id === id);
      if (!item) return;

      $("scheduleTitle").value = item.title;
      $("scheduleDate").value = item.date;
      $("schedulePlatform").value = item.platform;
      $("scheduleStatus").value = item.status;

      state.editingScheduleId = id;
      $("scheduleTitle").focus();
    }

    function deleteSchedule(id) {
      if (!confirm("Hapus jadwal ini?")) return;

      state.schedules = state.schedules.filter(item => item.id !== id);
      saveState();
      renderCalendar();
      renderDashboard();
    }

    function clearScheduleForm() {
      $("scheduleTitle").value = "";
      $("scheduleDate").value = "";
      $("scheduleStatus").value = "Planned";
      state.editingScheduleId = null;
    }

    function renderCalendar() {
      const now = new Date();
      const year = now.getFullYear();
      const month = now.getMonth();

      $("calendarMonth").textContent = now.toLocaleDateString("id-ID", {
        month: "long",
        year: "numeric"
      });

      const firstDay = new Date(year, month, 1).getDay();
      const daysInMonth = new Date(year, month + 1, 0).getDate();
      const today = new Date().getDate();

      const names = ["Min", "Sen", "Sel", "Rab", "Kam", "Jum", "Sab"];

      let html = names.map(name => `<div class="day-name">${name}</div>`).join("");

      for (let i = 0; i < firstDay; i++) {
        html += `<div></div>`;
      }

      for (let day = 1; day <= daysInMonth; day++) {
        const date = `${year}-${String(month + 1).padStart(2, "0")}-${String(day).padStart(2, "0")}`;
        const events = state.schedules.filter(item => item.date === date);

        html += `
          <div class="day ${day === today ? "today" : ""}">
            <strong>${day}</strong>
            ${events.map(event => `
              <div class="day-event" title="${escapeHtml(event.title)}">
                ${escapeHtml(event.title)}
              </div>
            `).join("")}
          </div>
        `;
      }

      $("calendarGrid").innerHTML = html;

      if (!state.schedules.length) {
        $("scheduleList").innerHTML = `<div class="empty">Belum ada jadwal konten.</div>`;
        return;
      }

      const sorted = [...state.schedules].sort((a, b) => a.date.localeCompare(b.date));

      $("scheduleList").innerHTML = `
        <div class="list">
          ${sorted.map(item => `
            <div class="list-item">
              <div>
                <strong>${escapeHtml(item.title)}</strong>
                <small>${item.date} · ${escapeHtml(item.platform)} · ${escapeHtml(item.status)}</small>
              </div>
              <div style="display:flex;gap:7px">
                <button class="btn" onclick="editSchedule(${item.id})">Edit</button>
                <button class="btn danger" onclick="deleteSchedule(${item.id})">Delete</button>
              </div>
            </div>
          `).join("")}
        </div>
      `;
    }

    function renderDashboard() {
      const revenue = state.revenue.monthlyUsd || 0;
      const yearly = state.revenue.yearlyUsd || 0;
      const engagement = state.revenue.engagement || 0;
      const expenses = state.expenses.reduce((sum, item) => sum + item.amount, 0);
      const profit = revenue - expenses;

      $("dashMonthly").textContent = money(revenue);
      $("dashYearly").textContent = money(yearly);
      $("dashEngagement").textContent = engagement.toFixed(2) + "%";
      $("dashProfit").textContent = money(profit);

      $("expenseCount").textContent = `${state.expenses.length} items`;
      $("scheduleCount").textContent = `${state.schedules.length} items`;

      $("dashboardExpenses").innerHTML = state.expenses.length
        ? `<div class="list">${state.expenses.slice(0, 4).map(item => `
            <div class="list-item">
              <div>
                <strong>${escapeHtml(item.name)}</strong>
                <small>${escapeHtml(item.category)}</small>
              </div>
              <strong class="negative">-${money(item.amount)}</strong>
            </div>
          `).join("")}</div>`
        : `<div class="empty">Belum ada pengeluaran.</div>`;

      $("dashboardSchedules").innerHTML = state.schedules.length
        ? `<div class="list">${state.schedules.slice(0, 4).map(item => `
            <div class="list-item">
              <div>
                <strong>${escapeHtml(item.title)}</strong>
                <small>${item.date} · ${escapeHtml(item.platform)}</small>
              </div>
              <span class="tag">${escapeHtml(item.status)}</span>
            </div>
          `).join("")}</div>`
        : `<div class="empty">Belum ada jadwal konten.</div>`;

      renderChart();
    }

    function renderChart() {
      const canvas = $("overviewChart");
      const ctx = canvas.getContext("2d");

      if (overviewChart) overviewChart.destroy();

      const revenue = state.revenue.monthlyUsd || 0;
      const expenses = state.expenses.reduce((sum, item) => sum + item.amount, 0);

      overviewChart = new Chart(ctx, {
        type: "bar",
        data: {
          labels: ["Revenue", "Expenses", "Net Profit"],
          datasets: [{
            label: "USD",
            data: [revenue, expenses, revenue - expenses],
            borderWidth: 1,
            borderRadius: 8,
            backgroundColor: [
              "rgba(216,180,254,.75)",
              "rgba(252,165,165,.65)",
              "rgba(134,239,172,.65)"
            ]
          }]
        },
        options: {
          responsive: true,
          maintainAspectRatio: false,
          resizeDelay: 100,
          layout: {
            padding: {
              top: 8,
              right: 8,
              bottom: 4,
              left: 4
            }
          },
          plugins: {
            legend: {
              display: false
            }
          },
          scales: {
            y: {
              beginAtZero: true,
              grid: {
                color: "rgba(255,255,255,.06)"
              },
              ticks: {
                color: "#aaa6b8",
                maxTicksLimit: 6
              }
            },
            x: {
              grid: {
                display: false
              },
              ticks: {
                color: "#aaa6b8",
                autoSkip: false,
                maxRotation: 0,
                minRotation: 0
              }
            }
          }
        }
      });
    }

    function exportExpenses() {
      const rows = [
        ["Name", "Category", "Amount USD", "Date"],
        ...state.expenses.map(item => [item.name, item.category, item.amount, item.date])
      ];

      downloadCSV(rows, "kreatordash-expenses.csv");
    }

    function exportAllData() {
      const rows = [
        ["Type", "Title", "Value", "Category", "Date"],
        ...state.expenses.map(item => ["Expense", item.name, item.amount, item.category, item.date]),
        ...state.scripts.map(item => ["Script", item.title, item.text, "", item.date]),
        ...state.schedules.map(item => ["Schedule", item.title, item.platform, item.status, item.date])
      ];

      downloadCSV(rows, "kreatordash-data.csv");
    }

    function downloadCSV(rows, filename) {
      const csv = rows
        .map(row => row.map(value => `"${String(value).replace(/"/g, '""')}"`).join(","))
        .join("\n");

      const blob = new Blob([csv], { type: "text/csv;charset=utf-8;" });
      const url = URL.createObjectURL(blob);
      const link = document.createElement("a");

      link.href = url;
      link.download = filename;
      link.click();

      URL.revokeObjectURL(url);
    }

    function saveSettings() {
      state.settings.name = $("creatorName").value.trim() || "Creator";
      state.settings.currency = $("displayCurrency").value;

      saveState();
      $("pageTitle").textContent = `Good day, ${state.settings.name}.`;
      alert("Settings berhasil disimpan.");
    }

    function clearAllData() {
      const confirmed = confirm(
        "Semua revenue, pengeluaran, script, dan jadwal akan dihapus. Lanjutkan?"
      );

      if (!confirmed) return;

      localStorage.removeItem("kd_revenue");
      localStorage.removeItem("kd_expenses");
      localStorage.removeItem("kd_scripts");
      localStorage.removeItem("kd_schedules");

      location.reload();
    }

    function formatDate(date) {
      return new Date(date).toLocaleDateString("id-ID", {
        day: "numeric",
        month: "short",
        year: "numeric"
      });
    }

    function escapeHtml(value) {
      return String(value)
        .replace(/&/g, "&amp;")
        .replace(/</g, "&lt;")
        .replace(/>/g, "&gt;")
        .replace(/"/g, "&quot;")
        .replace(/'/g, "&#039;");
    }

    function loadSavedData() {
      if (state.revenue.monthlyUsd !== undefined) {
        $("platform").value = state.revenue.platform || "TikTok";
        $("views").value = state.revenue.views || "";
        $("followers").value = state.revenue.followers || "";
        $("rpm").value = state.revenue.rpm || "0.50";
        $("likes").value = state.revenue.likes || "";
        $("comments").value = state.revenue.comments || "";
        $("shares").value = state.revenue.shares || "";

        $("monthlyResult").textContent = money(state.revenue.monthlyUsd);
        $("monthlyIdr").textContent = money(state.revenue.monthlyUsd * state.exchangeRate, "IDR");
        $("yearlyResult").textContent = money(state.revenue.yearlyUsd);
        $("yearlyIdr").textContent = money(state.revenue.yearlyUsd * state.exchangeRate, "IDR");
        $("engagementResult").textContent = (state.revenue.engagement || 0).toFixed(2) + "%";
      }

      $("creatorName").value = state.settings.name || "Creator";
      $("displayCurrency").value = state.settings.currency || "USD";
      $("pageTitle").textContent = `Good day, ${state.settings.name || "Creator"}.`;
    }

    loadSavedData();
    loadExchangeRate();
    renderDashboard();
    renderBudget();
    renderScripts();
    renderCalendar();

    setInterval(loadExchangeRate, 30 * 60 * 1000);
  </script>
  <!-- CREATOR BUSINESS HUB ADD-ON -->
<style>
  .business-fab {
    position: fixed;
    right: 22px;
    bottom: 22px;
    z-index: 9999;
    border: 0;
    border-radius: 999px;
    padding: 14px 18px;
    background: linear-gradient(135deg, #c084fc, #818cf8);
    color: white;
    font-weight: 800;
    cursor: pointer;
    box-shadow: 0 12px 35px rgba(129, 140, 248, .35);
  }

  .business-overlay {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 9998;
    background: rgba(0, 0, 0, .7);
    backdrop-filter: blur(8px);
    padding: 20px;
    overflow-y: auto;
  }

  .business-panel {
    width: min(1100px, 100%);
    margin: 30px auto;
    background: #11111b;
    border: 1px solid rgba(255,255,255,.12);
    border-radius: 24px;
    padding: 25px;
    color: #fff;
  }

  .business-head {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 15px;
    margin-bottom: 22px;
  }

  .business-head h2 {
    margin: 0;
    font-size: 24px;
  }

  .business-close {
    border: 0;
    background: rgba(255,255,255,.1);
    color: white;
    width: 38px;
    height: 38px;
    border-radius: 50%;
    cursor: pointer;
    font-size: 20px;
  }

  .business-tabs {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-bottom: 22px;
  }

  .business-tab {
    border: 1px solid rgba(255,255,255,.12);
    background: rgba(255,255,255,.06);
    color: #ddd;
    border-radius: 12px;
    padding: 10px 14px;
    cursor: pointer;
  }

  .business-tab.active {
    background: linear-gradient(135deg, #c084fc, #818cf8);
    color: white;
    border-color: transparent;
  }

  .business-view {
    display: none;
  }

  .business-view.active {
    display: block;
  }

  .business-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 14px;
  }

  .business-field {
    display: flex;
    flex-direction: column;
    gap: 7px;
  }

  .business-field.full {
    grid-column: 1 / -1;
  }

  .business-field label {
    font-size: 13px;
    color: #aaa;
  }

  .business-field input,
  .business-field select,
  .business-field textarea {
    width: 100%;
    box-sizing: border-box;
    padding: 12px;
    border-radius: 10px;
    border: 1px solid rgba(255,255,255,.12);
    background: #1b1b29;
    color: white;
    outline: none;
  }

  .business-field textarea {
    min-height: 90px;
    resize: vertical;
  }

  .business-btn {
    margin-top: 15px;
    border: 0;
    border-radius: 11px;
    padding: 12px 16px;
    background: linear-gradient(135deg, #c084fc, #818cf8);
    color: white;
    font-weight: 800;
    cursor: pointer;
  }

  .business-summary {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 12px;
    margin-bottom: 20px;
  }

  .business-stat {
    padding: 16px;
    border-radius: 15px;
    background: rgba(255,255,255,.06);
    border: 1px solid rgba(255,255,255,.08);
  }

  .business-stat small {
    display: block;
    color: #aaa;
    margin-bottom: 8px;
  }

  .business-stat strong {
    font-size: 22px;
  }

  .business-list {
    display: grid;
    gap: 12px;
    margin-top: 20px;
  }

  .business-item {
    padding: 16px;
    border-radius: 15px;
    background: rgba(255,255,255,.05);
    border: 1px solid rgba(255,255,255,.08);
  }

  .business-item-top {
    display: flex;
    justify-content: space-between;
    gap: 12px;
    align-items: flex-start;
  }

  .business-item h4 {
    margin: 0 0 6px;
  }

  .business-item p {
    margin: 4px 0;
    color: #aaa;
    font-size: 13px;
  }

  .business-status {
    display: inline-block;
    padding: 5px 9px;
    border-radius: 999px;
    font-size: 11px;
    font-weight: 800;
  }

  .status-paid {
    background: rgba(34,197,94,.16);
    color: #86efac;
  }

  .status-unpaid {
    background: rgba(250,204,21,.16);
    color: #fde047;
  }

  .status-overdue {
    background: rgba(239,68,68,.16);
    color: #fca5a5;
  }

  .business-delete {
    margin-top: 10px;
    border: 1px solid rgba(239,68,68,.3);
    background: transparent;
    color: #fca5a5;
    border-radius: 8px;
    padding: 7px 10px;
    cursor: pointer;
  }

  .business-empty {
    color: #999;
    text-align: center;
    padding: 25px;
  }

  .business-media-card {
    background: linear-gradient(135deg, #241936, #151827);
    border: 1px solid rgba(255,255,255,.12);
    border-radius: 20px;
    padding: 28px;
    margin-top: 20px;
  }

  .business-media-card h2 {
    margin-top: 0;
    font-size: 30px;
  }

  .business-media-card p {
    color: #c4b5fd;
  }

  /* ===== FIX RESPONSIVE MOBILE KREATOR DASH ===== */

@media (max-width: 768px) {

  html,
  body {
    width: 100%;
    max-width: 100%;
    overflow-x: hidden;
  }

  * {
    box-sizing: border-box;
  }

  /* Sidebar */
  .sidebar {
    position: relative;
    top: auto;
    left: auto;

    width: 100%;
    max-width: 100%;
    height: auto;
    min-height: auto;

    padding: 20px 16px;

    display: flex;
    flex-direction: column;
  }

  /* Konten utama */
  .main-content {
    position: relative;

    width: 100%;
    max-width: 100%;

    margin-left: 0;
    padding: 20px 16px;
  }

  /* Semua section */
  section,
  .content-section,
  .dashboard-section {
    width: 100%;
    max-width: 100%;
  }

  /* Grid dan kartu */
  .grid,
  .dashboard-grid,
  .stats-grid,
  .cards-grid {
    grid-template-columns: 1fr;
    width: 100%;
  }

  /* Tabel */
  .table-container {
    width: 100%;
    max-width: 100%;
    overflow-x: auto;
  }

  table {
    min-width: 600px;
  }

  /* Form */
  input,
  select,
  textarea,
  button {
    max-width: 100%;
  }
  
  /* Kalkulator */
  .calculator,
  .calculator-container {
    width: 100%;
    max-width: 100%;
  }

};
    }

    .business-panel {
      padding: 18px;
      margin: 10px auto;
    }

    .business-fab {
      right: 15px;
      bottom: 15px;
    }
<style>
/* ===== SIDEBAR FIX KREATOR DASH ===== */

.sidebar {
  position: fixed;
  top: 0;
  left: 0;
  z-index: 1000;

  width: 260px;
  height: 100vh;
  padding: 28px 18px;

  display: flex;
  flex-direction: column;

  background:
    linear-gradient(
      180deg,
      rgba(19, 16, 43, 0.98),
      rgba(10, 9, 24, 0.98)
    );

  border-right: 1px solid rgba(139, 92, 246, 0.22);
  box-shadow: 12px 0 35px rgba(0, 0, 0, 0.18);

  overflow-y: auto;
  overflow-x: hidden;
}

.sidebar .logo {
  margin: 0 0 36px;
  padding: 0 12px;

  font-size: 25px;
  font-weight: 800;
  letter-spacing: -1px;
  white-space: nowrap;
}

.sidebar .logo span {
  color: #a78bfa;
}

.sidebar .nav,
.sidebar nav {
  display: flex;
  flex-direction: column;
  gap: 8px;
  width: 100%;
}

.sidebar button,
.sidebar .nav-button,
.sidebar .nav button {
  width: 100%;
  min-height: 46px;

  display: flex;
  align-items: center;
  gap: 12px;

  padding: 12px 14px;

  border: 1px solid transparent;
  border-radius: 13px;

  background: transparent;
  color: #aaa8c5;

  font: inherit;
  font-size: 14px;
  font-weight: 600;
  text-align: left;

  cursor: pointer;

  transition:
    background .2s ease,
    color .2s ease,
    border-color .2s ease,
    transform .2s ease;
}

.sidebar button:hover,
.sidebar .nav-button:hover,
.sidebar .nav button:hover {
  color: #ffffff;
  background: rgba(139, 92, 246, 0.12);
  border-color: rgba(139, 92, 246, 0.18);
  transform: translateX(3px);
}

.sidebar button.active,
.sidebar .nav-button.active,
.sidebar .nav button.active {
  color: #ffffff;

  background:
    linear-gradient(
      135deg,
      rgba(139, 92, 246, 0.32),
      rgba(99, 102, 241, 0.18)
    );

  border-color: rgba(167, 139, 250, 0.35);

  box-shadow:
    inset 3px 0 0 #a78bfa,
    0 8px 20px rgba(76, 29, 149, 0.12);
}

.sidebar button:focus-visible {
  outline: 2px solid #a78bfa;
  outline-offset: 3px;
}

/* Konten utama */
.main {
  margin-left: 260px;
  width: calc(100% - 260px);
}

/* Tampilan HP */
@media (max-width: 700px) {
  .sidebar {
    width: 270px;
    height: 100vh;
    position: fixed;

    transform: translateX(-110%);
    transition: transform .25s ease;
  }

  .sidebar.open {
    transform: translateX(0);
  }

  .main {
    width: 100%;
    margin-left: 0;
  }
}
  .dashboard-grid,
  .stats-grid,
  .cards-grid {
    grid-template-columns: 1fr !important;
  }

  .card {
    width: 100%;
    max-width: 100%;
  }

  .topbar {
    flex-direction: column;
    align-items: flex-start;
    gap: 12px;
  }

  table {
    display: block;
    width: 100%;
    overflow-x: auto;
  }

  button,
  input,
  select,
  textarea {
    max-width: 100%;
  }
}
  }
</style>

<button class="business-fab" onclick="openBusinessHub()">
  Business Hub
</button>

<div class="business-overlay" id="businessOverlay">
  <div class="business-panel">
    <div class="business-head">
      <div>
        <h2>Creator Business Hub</h2>
        <p style="color:#aaa;margin:6px 0 0;">
          Kelola brand deal, invoice, pembayaran, dan media kit.
        </p>
      </div>
      <button class="business-close" onclick="closeBusinessHub()">×</button>
    </div>

    <div class="business-tabs">
      <button class="business-tab active" onclick="switchBusinessTab('deals', this)">
        Brand Deals
      </button>
      <button class="business-tab" onclick="switchBusinessTab('brands', this)">
        Brand CRM
      </button>
      <button class="business-tab" onclick="switchBusinessTab('media', this)">
        Media Kit
      </button>
    </div>

    <!-- BRAND DEALS -->
    <section class="business-view active" id="businessDeals">
      <div class="business-summary">
        <div class="business-stat">
          <small>Total Deal</small>
          <strong id="businessTotalDeals">0</strong>
        </div>
        <div class="business-stat">
          <small>Sudah Dibayar</small>
          <strong id="businessPaidTotal">$0</strong>
        </div>
        <div class="business-stat">
          <small>Belum Dibayar</small>
          <strong id="businessUnpaidTotal">$0</strong>
        </div>
      </div>

      <form id="businessDealForm">
        <div class="business-grid">
          <div class="business-field">
            <label>Nama Brand</label>
            <input id="dealBrand" required placeholder="Contoh: Nike">
          </div>

          <div class="business-field">
            <label>Nama Campaign</label>
            <input id="dealCampaign" required placeholder="Contoh: Summer Campaign">
          </div>

          <div class="business-field">
            <label>Nilai Deal USD</label>
            <input id="dealAmount" type="number" min="0" required placeholder="500">
          </div>

          <div class="business-field">
            <label>Deadline Pembayaran</label>
            <input id="dealDueDate" type="date" required>
          </div>

          <div class="business-field">
            <label>Status</label>
            <select id="dealStatus">
              <option value="unpaid">Belum Dibayar</option>
              <option value="paid">Sudah Dibayar</option>
            </select>
          </div>

          <div class="business-field">
            <label>Link Invoice / Kontrak</label>
            <input id="dealLink" type="url" placeholder="https://...">
          </div>

          <div class="business-field full">
            <label>Catatan</label>
            <textarea id="dealNotes" placeholder="Brief, deliverables, revisi, dan catatan lainnya"></textarea>
          </div>
        </div>

        <button class="business-btn" type="submit">
          + Tambah Brand Deal
        </button>
      </form>

      <div class="business-list" id="businessDealList"></div>
    </section>

    <!-- BRAND CRM -->
    <section class="business-view" id="businessBrands">
      <form id="businessBrandForm">
        <div class="business-grid">
          <div class="business-field">
            <label>Nama Brand</label>
            <input id="crmName" required placeholder="Contoh: Adidas">
          </div>

          <div class="business-field">
            <label>Nama Kontak</label>
            <input id="crmContact" placeholder="Contoh: Sarah">
          </div>

          <div class="business-field">
            <label>Email</label>
            <input id="crmEmail" type="email" placeholder="brand@email.com">
          </div>

          <div class="business-field">
            <label>Website</label>
            <input id="crmWebsite" type="url" placeholder="https://...">
          </div>

          <div class="business-field full">
            <label>Catatan</label>
            <textarea id="crmNotes" placeholder="Riwayat komunikasi, niche brand, peluang kerja sama, dan lain-lain"></textarea>
          </div>
        </div>

        <button class="business-btn" type="submit">
          + Tambah Brand
        </button>
      </form>

      <div class="business-list" id="businessBrandList"></div>
    </section>

    <!-- MEDIA KIT -->
    <section class="business-view" id="businessMedia">
      <form id="businessMediaForm">
        <div class="business-grid">
          <div class="business-field">
            <label>Nama Creator</label>
            <input id="mediaName" placeholder="Nama kamu" required>
          </div>

          <div class="business-field">
            <label>Niche</label>
            <input id="mediaNiche" placeholder="Beauty, fashion, lifestyle">
          </div>

          <div class="business-field">
            <label>Jumlah Followers</label>
            <input id="mediaFollowers" type="number" placeholder="10000">
          </div>

          <div class="business-field">
            <label>Rata-rata Views</label>
            <input id="mediaViews" type="number" placeholder="25000">
          </div>

          <div class="business-field">
            <label>Email Bisnis</label>
            <input id="mediaEmail" type="email" placeholder="hello@yourname.com">
          </div>

          <div class="business-field">
            <label>Instagram / TikTok</label>
            <input id="mediaSocial" placeholder="@username">
          </div>
        </div>

        <button class="business-btn" type="submit">
          Buat Media Kit
        </button>
      </form>

      <div id="businessMediaResult"></div>
    </section>
  </div>
</div>

<script>
  const businessStorage = {
    deals: "creator_business_deals",
    brands: "creator_business_brands",
    media: "creator_business_media"
  };

  let businessDeals = JSON.parse(
    localStorage.getItem(businessStorage.deals) || "[]"
  );

  let businessBrands = JSON.parse(
    localStorage.getItem(businessStorage.brands) || "[]"
  );

  let businessMedia = JSON.parse(
    localStorage.getItem(businessStorage.media) || "null"
  );

  function openBusinessHub() {
    document.getElementById("businessOverlay").style.display = "block";
    renderBusinessDeals();
    renderBusinessBrands();
    renderBusinessMedia();
  }

  function closeBusinessHub() {
    document.getElementById("businessOverlay").style.display = "none";
  }

  function switchBusinessTab(tab, button) {
    document.querySelectorAll(".business-tab").forEach(item => {
      item.classList.remove("active");
    });

    document.querySelectorAll(".business-view").forEach(item => {
      item.classList.remove("active");
    });

    button.classList.add("active");
    document.getElementById("business" + tab.charAt(0).toUpperCase() + tab.slice(1))
      .classList.add("active");
  }

  function saveBusinessData() {
    localStorage.setItem(
      businessStorage.deals,
      JSON.stringify(businessDeals)
    );

    localStorage.setItem(
      businessStorage.brands,
      JSON.stringify(businessBrands)
    );

    localStorage.setItem(
      businessStorage.media,
      JSON.stringify(businessMedia)
    );
  }

  function moneyUSD(value) {
    return "$" + Number(value || 0).toLocaleString("en-US", {
      minimumFractionDigits: 0,
      maximumFractionDigits: 2
    });
  }

  function getDealStatus(deal) {
    if (deal.status === "paid") return "paid";

    if (
      deal.dueDate &&
      new Date(deal.dueDate + "T23:59:59") < new Date()
    ) {
      return "overdue";
    }

    return "unpaid";
  }

  function renderBusinessDeals() {
    const list = document.getElementById("businessDealList");

    const paid = businessDeals
      .filter(deal => getDealStatus(deal) === "paid")
      .reduce((sum, deal) => sum + Number(deal.amount || 0), 0);

    const unpaid = businessDeals
      .filter(deal => getDealStatus(deal) !== "paid")
      .reduce((sum, deal) => sum + Number(deal.amount || 0), 0);

    document.getElementById("businessTotalDeals").textContent =
      businessDeals.length;

    document.getElementById("businessPaidTotal").textContent =
      moneyUSD(paid);

    document.getElementById("businessUnpaidTotal").textContent =
      moneyUSD(unpaid);

    if (!businessDeals.length) {
      list.innerHTML = `
        <div class="business-empty">
          Belum ada brand deal. Tambahkan deal pertamamu.
        </div>
      `;
      return;
    }

    list.innerHTML = businessDeals.map((deal, index) => {
      const status = getDealStatus(deal);

      return `
        <div class="business-item">
          <div class="business-item-top">
            <div>
              <h4>${escapeBusiness(deal.brand)}</h4>
              <p>${escapeBusiness(deal.campaign)}</p>
              <p>Deadline: ${escapeBusiness(deal.dueDate)}</p>
              <p>Nilai: <strong>${moneyUSD(deal.amount)}</strong></p>
            </div>

            <span class="business-status status-${status}">
              ${
                status === "paid"
                  ? "PAID"
                  : status === "overdue"
                  ? "OVERDUE"
                  : "UNPAID"
              }
            </span>
          </div>

          ${
            deal.notes
              ? `<p>${escapeBusiness(deal.notes)}</p>`
              : ""
          }

          ${
            deal.link
              ? `<p><a href="${escapeBusiness(deal.link)}" target="_blank" style="color:#c4b5fd;">Buka Link</a></p>`
              : ""
          }

          <button class="business-delete" onclick="deleteBusinessDeal(${index})">
            Hapus Deal
          </button>
        </div>
      `;
    }).join("");
  }

  function renderBusinessBrands() {
    const list = document.getElementById("businessBrandList");

    if (!businessBrands.length) {
      list.innerHTML = `
        <div class="business-empty">
          Belum ada brand di CRM kamu.
  const businessStorage = {
    deals: "creator_business_deals",
    brands: "creator_business_brands",
    media: "creator_business_media"
  };
  
</body>
</html>


<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<style>
@import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700;800&display=swap');

:root {
  --bg: #080812;
  --panel: #111121;
  --panel2: #17172c;
  --border: #292642;
  --text: #f8f7ff;
  --muted: #9995b6;
  --purple: #8b5cf6;
  --purple2: #a78bfa;
  --pink: #ec4899;
  --green: #34d399;
  --yellow: #fbbf24;
  --red: #fb7185;
  --radius: 20px;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: "DM Sans", sans-serif;
  background:
    radial-gradient(circle at top right, #25144d 0, transparent 35%),
    var(--bg);
  color: var(--text);
}

button,
input,
select,
textarea {
  font: inherit;
}

button {
  cursor: pointer;
}

.hidden {
  display: none !important;
}

.app {
  display: flex;
  min-height: 100vh;
}

.sidebar {
  width: 245px;
  background: rgba(12, 12, 27, .96);
  border-right: 1px solid var(--border);
  padding: 24px 16px;
  position: fixed;
  inset: 0 auto 0 0;
  z-index: 20;
}

.logo {
  font-size: 24px;
  font-weight: 800;
  margin-bottom: 35px;
}

.logo span {
  color: var(--purple2);
}

.nav-button {
  width: 100%;
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 13px 14px;
  margin-bottom: 7px;
  border: 0;
  border-radius: 13px;
  background: transparent;
  color: var(--muted);
  text-align: left;
}

.nav-button:hover,
.nav-button.active {
  background: linear-gradient(135deg, #30205a, #20152f);
  color: white;
}

.main {
  margin-left: 245px;
  width: calc(100% - 245px);
  padding: 28px;
}

.topbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 20px;
  margin-bottom: 28px;
}

.topbar h1 {
  margin: 0;
  font-size: 30px;
}

.topbar p {
  color: var(--muted);
  margin: 7px 0 0;
}

.profile {
  display: flex;
  align-items: center;
  gap: 12px;
}

.avatar {
  width: 43px;
  height: 43px;
  border-radius: 50%;
  display: grid;
  place-items: center;
  background: linear-gradient(135deg, var(--purple), var(--pink));
  font-weight: 800;
}

.page {
  display: none;
}

.page.active {
  display: block;
}

.grid {
  display: grid;
  gap: 18px;
}

.grid-4 {
  grid-template-columns: repeat(4, 1fr);
}

.grid-3 {
  grid-template-columns: repeat(3, 1fr);
}

.grid-2 {
  grid-template-columns: repeat(2, 1fr);
}

.card {
  background: linear-gradient(145deg, rgba(24, 23, 45, .98), rgba(13, 13, 28, .98));
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 21px;
  box-shadow: 0 15px 50px rgba(0,0,0,.15);
}

.stat-label {
  color: var(--muted);
  font-size: 13px;
}

.stat-value {
  font-size: 29px;
  font-weight: 800;
  margin-top: 10px;
}

.stat-small {
  font-size: 12px;
  color: var(--green);
  margin-top: 8px;
}

.section-title {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 15px;
  margin: 28px 0 15px;
}

.section-title h2 {
  margin: 0;
  font-size: 20px;
}

.btn {
  border: 0;
  border-radius: 12px;
  padding: 11px 15px;
  background: var(--purple);
  color: white;
  font-weight: 700;
}

.btn:hover {
  filter: brightness(1.15);
}

.btn.secondary {
  background: #292442;
}

.btn.danger {
  background: #542033;
}

.btn.green {
  background: #14533f;
}

.form-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 13px;
}

.form-group {
  display: flex;
  flex-direction: column;
  gap: 7px;
}

.form-group.full {
  grid-column: 1 / -1;
}

label {
  color: var(--muted);
  font-size: 13px;
}

input,
select,
textarea {
  width: 100%;
  padding: 12px;
  background: #0b0b19;
  color: white;
  border: 1px solid var(--border);
  border-radius: 11px;
  outline: none;
}

input:focus,
select:focus,
textarea:focus {
  border-color: var(--purple);
}

textarea {
  min-height: 90px;
  resize: vertical;
}

.actions {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  margin-top: 15px;
}

.pipeline {
  display: grid;
  grid-template-columns: repeat(6, minmax(170px, 1fr));
  gap: 13px;
  overflow-x: auto;
  padding-bottom: 10px;
}

.pipeline-column {
  background: #0c0c19;
  border: 1px solid var(--border);
  border-radius: 15px;
  min-height: 230px;
  padding: 12px;
}

.pipeline-column h3 {
  font-size: 13px;
  color: var(--purple2);
  margin: 0 0 12px;
}

.content-card {
  background: var(--panel2);
  border: 1px solid #302b4b;
  border-radius: 12px;
  padding: 12px;
  margin-bottom: 10px;
  cursor: grab;
}

.content-card strong {
  display: block;
  margin-bottom: 8px;
}

.content-card small {
  color: var(--muted);
}

.badge {
  display: inline-block;
  padding: 5px 8px;
  border-radius: 8px;
  background: #292344;
  color: var(--purple2);
  font-size: 11px;
  margin-top: 8px;
}

.table-wrap {
  overflow-x: auto;
}

table {
  width: 100%;
  border-collapse: collapse;
  min-width: 650px;
}

th,
td {
  padding: 14px 10px;
  border-bottom: 1px solid var(--border);
  text-align: left;
  font-size: 13px;
}

th {
  color: var(--muted);
  font-weight: 600;
}

.empty {
  text-align: center;
  padding: 35px;
  color: var(--muted);
}

.search-row {
  display: flex;
  gap: 10px;
  margin-bottom: 18px;
}

.search-row input {
  flex: 1;
}

.idea-card,
.deal-card {
  background: #0d0d1d;
  border: 1px solid var(--border);
  padding: 17px;
  border-radius: 15px;
}

.idea-card h3,
.deal-card h3 {
  margin-top: 0;
}

.idea-card p,
.deal-card p {
  color: var(--muted);
  font-size: 13px;
}

.media-preview {
  background: linear-gradient(145deg, #25154b, #111121);
  border: 1px solid #5b3b9a;
  border-radius: 18px;
  padding: 28px;
  min-height: 320px;
}

.media-preview h1 {
  font-size: 32px;
  margin-bottom: 8px;
}

.media-preview .media-name {
  color: var(--purple2);
  font-size: 16px;
}

.media-stat {
  display: inline-block;
  margin: 18px 12px 0 0;
  padding: 12px;
  background: rgba(0,0,0,.25);
  border-radius: 12px;
}

.login-screen {
  min-height: 90vh;
  display: grid;
  place-items: center;
  padding: 20px;
  background:
    radial-gradient(circle at top right, #492080, transparent 35%),
    #080812;
}

.login-box {
  width: 100%;
  max-width: 420px;
  background: #111121;
  border: 1px solid var(--border);
  border-radius: 24px;
  padding: 30px;
}

.login-box h1 {
  margin-top: 0;
  font-size: 32px;
}

.login-box p {
  color: var(--muted);
}

.mobile-toggle {
  display: none;
}

@media(max-width: 1100px) {
  .grid-4 {
    grid-template-columns: repeat(2, 1fr);
  }

  .pipeline {
    grid-template-columns: repeat(6, 190px);
  }
}

@media(max-width: 700px) {
  .sidebar {
    transform: translateX(-100%);
    transition: .25s;
  }

  .sidebar.open {
    transform: translateX(0);
  }

  .main {
    margin-left: 0;
    width: 100%;
    padding: 18px;
  }

  .mobile-toggle {
    display: block;
    background: #292442;
    color: white;
    border: 0;
    border-radius: 10px;
    padding: 10px;
  }

  .topbar {
    align-items: flex-start;
  }

  .topbar h1 {
    font-size: 24px;
  }

  .profile {
    display: none;
  }

  .grid-4,
  .grid-3,
  .grid-2,
  .form-grid {
    grid-template-columns: 1fr;
  }

  .section-title {
    align-items: flex-start;
    flex-direction: column;
  }
}
</style>
</head>

<body>

<div id="loginScreen" class="login-screen">
  <div class="login-box">
    <div class="logo">Kreator<span>Dash</span></div>
    <h1>Selamat datang kembali</h1>
    <p>Kelola konten, brand deal, pendapatan, dan media kit dalam satu tempat.</p>

    <form id="loginForm">
      <div class="form-group">
        <label>Email</label>
        <input id="loginEmail" type="email" placeholder="nama@email.com" required>
      </div>

      <br>

      <div class="form-group">
        <label>Password</label>
        <input id="loginPassword" type="password" placeholder="Masukkan password" required>
      </div>

      <br>

      <button class="btn" style="width:100%">Masuk ke KreatorDash</button>
    </form>
  </div>
</div>

<div id="app" class="app hidden">

  <aside id="sidebar" class="sidebar">
    <div class="logo">Kreator<span>Dash</span></div>

    <button class="nav-button active" data-page="dashboard">◈ Dashboard</button>
    <button class="nav-button" data-page="pipeline">▦ Content Pipeline</button>
    <button class="nav-button" data-page="schedule">◷ Schedule</button>
    <button class="nav-button" data-page="revenue">◉ Revenue</button>
    <button class="nav-button" data-page="deals">♢ Brand Deals</button>
    <button class="nav-button" data-page="ideas">✦ Content Ideas</button>
    <button class="nav-button" data-page="media">▣ Media Kit</button>

    <button class="nav-button" id="logoutButton" style="margin-top:30px">↪ Logout</button>
  </aside>

  <main class="main">

    <div class="topbar">
      <div>
        <button class="mobile-toggle" id="mobileToggle">☰</button>
        <h1 id="pageTitle">Dashboard</h1>
        <p id="pageSubtitle">Pantau aktivitas kreator kamu hari ini.</p>
      </div>

      <div class="profile">
        <div>
          <strong id="userEmail">Creator</strong>
          <small style="display:block;color:var(--muted)">Creator Account</small>
        </div>
        <div class="avatar" id="avatar">C</div>
      </div>
    </div>

    <section id="dashboard" class="page active">
      <div class="grid grid-4">
        <div class="card">
          <div class="stat-label">Total Konten</div>
          <div class="stat-value" id="statContents">0</div>
          <div class="stat-small">Semua konten kamu</div>
        </div>

        <div class="card">
          <div class="stat-label">Konten Published</div>
          <div class="stat-value" id="statPublished">0</div>
          <div class="stat-small">Konten selesai</div>
        </div>

        <div class="card">
          <div class="stat-label">Total Views</div>
          <div class="stat-value" id="statViews">0</div>
          <div class="stat-small">Dari seluruh platform</div>
        </div>

        <div class="card">
          <div class="stat-label">Total Revenue</div>
          <div class="stat-value" id="statRevenue">Rp0</div>
          <div class="stat-small">Pendapatan tercatat</div>
        </div>
      </div>

      <div class="section-title">
        <h2>Analytics</h2>
      </div>

      <div class="grid grid-2">
        <div class="card">
          <canvas id="contentChart"></canvas>
        </div>

        <div class="card">
          <canvas id="revenueChart"></canvas>
        </div>
      </div>

      <div class="section-title">
        <h2>Aktivitas Terbaru</h2>
      </div>

      <div class="card" id="recentActivity"></div>
    </section>

    <section id="pipeline" class="page">
      <div class="section-title">
        <h2>Content Pipeline</h2>
        <button class="btn" onclick="openContentForm()">+ Tambah Konten</button>
      </div>

      <div id="contentFormBox" class="card hidden">
        <h2 id="contentFormTitle">Tambah Konten</h2>

        <form id="contentForm">
          <input type="hidden" id="contentId">

          <div class="form-grid">
            <div class="form-group">
              <label>Judul Konten</label>
              <input id="contentTitle" required>
            </div>

            <div class="form-group">
              <label>Platform</label>
              <select id="contentPlatform">
                <option>TikTok</option>
                <option>Instagram</option>
                <option>YouTube</option>
                <option>Facebook</option>
              </select>
            </div>

            <div class="form-group">
              <label>Tanggal Publikasi</label>
              <input id="contentDate" type="date">
            </div>

            <div class="form-group">
              <label>Views</label>
              <input id="contentViews" type="number" min="0" value="0">
            </div>

            <div class="form-group">
              <label>Likes</label>
              <input id="contentLikes" type="number" min="0" value="0">
            </div>

            <div class="form-group">
              <label>Comments</label>
              <input id="contentComments" type="number" min="0" value="0">
            </div>

            <div class="form-group full">
              <label>Catatan</label>
              <textarea id="contentNotes"></textarea>
            </div>
          </div>

          <div class="actions">
            <button class="btn">Simpan Konten</button>
            <button type="button" class="btn secondary" onclick="closeContentForm()">Batal</button>
          </div>
        </form>
      </div>

      <div class="pipeline" id="pipelineBoard"></div>
    </section>

    <section id="schedule" class="page">
      <div class="section-title">
        <h2>Content Schedule</h2>
        <button class="btn" onclick="goToPage('pipeline')">Atur dari Pipeline</button>
      </div>

      <div class="card">
        <div class="form-group">
          <label>Pilih tanggal</label>
          <input type="date" id="scheduleDate" onchange="renderSchedule()">
        </div>
      </div>

      <div class="section-title">
        <h2>Jadwal Konten</h2>
      </div>

      <div id="scheduleList" class="grid grid-2"></div>
    </section>

    <section id="revenue" class="page">
      <div class="section-title">
        <h2>Revenue Tracker</h2>
        <button class="btn" onclick="openRevenueForm()">+ Tambah Revenue</button>
      </div>

      <div id="revenueFormBox" class="card hidden">
        <h2>Data Revenue</h2>

        <form id="revenueForm">
          <input type="hidden" id="revenueId">

          <div class="form-grid">
            <div class="form-group">
              <label>Platform</label>
              <select id="revenuePlatform">
                <option>TikTok</option>
                <option>YouTube</option>
                <option>Facebook</option>
                <option>Instagram</option>
                <option>Brand Deal</option>
              </select>
            </div>

            <div class="form-group">
              <label>Jumlah Pendapatan</label>
              <input id="revenueAmount" type="number" min="0" required>
            </div>

            <div class="form-group">
              <label>Mata Uang</label>
              <select id="revenueCurrency">
                <option>IDR</option>
                <option>USD</option>
                <option>EUR</option>
                <option>SGD</option>
                <option>MYR</option>
              </select>
            </div>

            <div class="form-group">
              <label>Total Views</label>
              <input id="revenueViews" type="number" min="0" value="0">
            </div>

            <div class="form-group">
              <label>Tanggal</label>
              <input id="revenueDate" type="date">
            </div>

            <div class="form-group full">
              <label>Catatan</label>
              <textarea id="revenueNotes"></textarea>
            </div>
          </div>

          <div class="actions">
            <button class="btn">Simpan Revenue</button>
            <button type="button" class="btn secondary" onclick="closeRevenueForm()">Batal</button>
          </div>
        </form>
      </div>

      <div class="card table-wrap">
        <table>
          <thead>
            <tr>
              <th>Platform</th>
              <th>Jumlah</th>
              <th>Views</th>
              <th>Tanggal</th>
              <th>Aksi</th>
            </tr>
          </thead>
          <tbody id="revenueTable"></tbody>
        </table>
      </div>
    </section>

    <section id="deals" class="page">
      <div class="section-title">
        <h2>Brand Deals</h2>
        <button class="btn" onclick="openDealForm()">+ Tambah Deal</button>
      </div>

      <div id="dealFormBox" class="card hidden">
        <h2>Brand Deal</h2>

        <form id="dealForm">
          <input type="hidden" id="dealId">

          <div class="form-grid">
            <div class="form-group">
              <label>Nama Brand</label>
              <input id="dealBrand" required>
            </div>

            <div class="form-group">
              <label>Nama Campaign</label>
              <input id="dealCampaign" required>
            </div>

            <div class="form-group">
              <label>Nilai Deal</label>
              <input id="dealValue" type="number" min="0">
            </div>

            <div class="form-group">
              <label>Mata Uang</label>
              <select id="dealCurrency">
                <option>IDR</option>
                <option>USD</option>
                <option>EUR</option>
              </select>
            </div>

            <div class="form-group">
              <label>Deadline</label>
              <input id="dealDeadline" type="date">
            </div>

            <div class="form-group">
              <label>Status Deal</label>
              <select id="dealStatus">
                <option>Negotiation</option>
                <option>Approved</option>
                <option>In Progress</option>
                <option>Completed</option>
                <option>Cancelled</option>
              </select>
            </div>

            <div class="form-group">
              <label>Status Pembayaran</label>
              <select id="dealPayment">
                <option>Unpaid</option>
                <option>Partially Paid</option>
                <option>Paid</option>
              </select>
            </div>

            <div class="form-group full">
              <label>Catatan</label>
              <textarea id="dealNotes"></textarea>
            </div>
          </div>

          <div class="actions">
            <button class="btn">Simpan Deal</button>
            <button type="button" class="btn secondary" onclick="closeDealForm()">Batal</button>
          </div>
        </form>
      </div>

      <div id="dealList" class="grid grid-2"></div>
    </section>

    <section id="ideas" class="page">
      <div class="section-title">
        <h2>Content Ideas</h2>
      </div>

      <div class="card">
        <div class="search-row">
          <input id="ideaKeyword" placeholder="Contoh: ide konten skincare">
          <button class="btn" onclick="searchIdeas()">Cari</button>
        </div>

        <div class="actions">
          <button class="btn secondary" onclick="externalSearch('google')">Google</button>
          <button class="btn secondary" onclick="externalSearch('youtube')">YouTube</button>
          <button class="btn secondary" onclick="externalSearch('tiktok')">TikTok</button>
          <button class="btn secondary" onclick="externalSearch('pinterest')">Pinterest</button>
        </div>
      </div>

      <div class="section-title">
        <h2>Simpan Ide</h2>
        <button class="btn" onclick="openIdeaForm()">+ Tambah Ide</button>
      </div>

      <div id="ideaFormBox" class="card hidden">
        <form id="ideaForm">
          <input type="hidden" id="ideaId">

          <div class="form-grid">
            <div class="form-group">
              <label>Judul Ide</label>
              <input id="ideaTitle" required>
            </div>

            <div class="form-group">
              <label>Platform</label>
              <select id="ideaPlatform">
                <option>TikTok</option>
                <option>YouTube</option>
                <option>Instagram</option>
                <option>Facebook</option>
              </select>
            </div>

            <div class="form-group">
              <label>Kategori</label>
              <input id="ideaCategory">
            </div>

            <div class="form-group">
              <label>Status</label>
              <select id="ideaStatus">
                <option>Saved</option>
                <option>Planning</option>
                <option>Created</option>
                <option>Published</option>
              </select>
            </div>

            <div class="form-group full">
              <label>Catatan</label>
              <textarea id="ideaNotes"></textarea>
            </div>
          </div>

          <div class="actions">
            <button class="btn">Simpan Ide</button>
            <button type="button" class="btn secondary" onclick="closeIdeaForm()">Batal</button>
          </div>
        </form>
      </div>

      <div id="ideaList" class="grid grid-2"></div>
    </section>

    <section id="media" class="page">
      <div class="section-title">
        <h2>Media Kit</h2>
        <button class="btn" onclick="window.print()">Print / Save PDF</button>
      </div>

      <div class="grid grid-2">
        <div class="card">
          <form id="mediaForm">
            <div class="form-group">
              <label>Nama Kreator</label>
              <input id="mediaName">
            </div>

            <br>

            <div class="form-group">
              <label>Username</label>
              <input id="mediaUsername">
            </div>

            <br>

            <div class="form-group">
              <label>Niche</label>
              <input id="mediaNiche" placeholder="Lifestyle, beauty, education...">
            </div>

            <br>

            <div class="form-group">
              <label>Bio</label>
              <textarea id="mediaBio"></textarea>
            </div>

            <br>

            <div class="form-group">
              <label>Total Followers</label>
              <input id="mediaFollowers" type="number" min="0">
            </div>

            <br>

            <div class="form-group">
              <label>Engagement Rate (%)</label>
              <input id="mediaEngagement" type="number" min="0" step="0.1">
            </div>

            <br>

            <button class="btn">Update Media Kit</button>
          </form>
        </div>

        <div class="media-preview" id="mediaPreview"></div>
      </div>
    </section>

  </main>
</div>

<script>
const STORAGE_KEY = "kreatordash_complete_v1";
const LOGIN_KEY = "kreatordash_login_v1";

const statuses = [
  "Ide",
  "Draft",
  "Editing",
  "Review",
  "Scheduled",
  "Published"
];

let data = JSON.parse(localStorage.getItem(STORAGE_KEY) || "null") || {
  contents: [],
  revenues: [],
  deals: [],
  ideas: [],
  media: {
    name: "Your Name",
    username: "@username",
    niche: "Content Creator",
    bio: "Creative storyteller and digital creator.",
    followers: 0,
    engagement: 0
  }
};

let charts = {};

function saveData() {
  localStorage.setItem(STORAGE_KEY, JSON.stringify(data));
  renderAll();
}

function uid(prefix) {
  return prefix + "-" + Date.now() + "-" + Math.random().toString(36).slice(2, 7);
}

function money(amount, currency = "IDR") {
  return new Intl.NumberFormat("id-ID", {
    style: "currency",
    currency,
    maximumFractionDigits: 0
  }).format(Number(amount) || 0);
}

function number(value) {
  return new Intl.NumberFormat("id-ID").format(Number(value) || 0);
}

function escapeHTML(value) {
  return String(value || "")
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#039;");
}

function goToPage(page) {
  document.querySelectorAll(".page").forEach(el => {
    el.classList.toggle("active", el.id === page);
  });

  document.querySelectorAll(".nav-button[data-page]").forEach(btn => {
    btn.classList.toggle("active", btn.dataset.page === page);
  });

  const titles = {
    dashboard: ["Dashboard", "Pantau aktivitas kreator kamu hari ini."],
    pipeline: ["Content Pipeline", "Atur perjalanan setiap konten dari ide sampai publish."],
    schedule: ["Schedule", "Lihat jadwal publikasi konten kamu."],
    revenue: ["Revenue", "Catat dan kelola semua pendapatan kreator."],
    deals: ["Brand Deals", "Kelola kerja sama dan pembayaran brand."],
    ideas: ["Content Ideas", "Simpan dan kembangkan ide konten baru."],
    media: ["Media Kit", "Tampilkan profil dan statistik kreator kamu."]
  };

  document.getElementById("pageTitle").textContent = titles[page][0];
  document.getElementById("pageSubtitle").textContent = titles[page][1];
  document.getElementById("sidebar").classList.remove("open");
}

document.querySelectorAll(".nav-button[data-page]").forEach(btn => {
  btn.addEventListener("click", () => goToPage(btn.dataset.page));
});

document.getElementById("mobileToggle").addEventListener("click", () => {
  document.getElementById("sidebar").classList.toggle("open");
});

document.getElementById("loginForm").addEventListener("submit", e => {
  e.preventDefault();

  const email = document.getElementById("loginEmail").value.trim();

  localStorage.setItem(LOGIN_KEY, email);
  showApp();
});

document.getElementById("logoutButton").addEventListener("click", () => {
  localStorage.removeItem(LOGIN_KEY);
  location.reload();
});

function showApp() {
  const email = localStorage.getItem(LOGIN_KEY);

  if (!email) return;

  document.getElementById("loginScreen").classList.add("hidden");
  document.getElementById("app").classList.remove("hidden");
  document.getElementById("userEmail").textContent = email;
  document.getElementById("avatar").textContent = email[0].toUpperCase();

  renderAll();
}

function openContentForm(id = "") {
  document.getElementById("contentFormBox").classList.remove("hidden");
  document.getElementById("contentForm").reset();
  document.getElementById("contentId").value = "";

  if (id) {
    const item = data.contents.find(x => x.id === id);
    if (!item) return;

    document.getElementById("contentFormTitle").textContent = "Edit Konten";
    document.getElementById("contentId").value = item.id;
    document.getElementById("contentTitle").value = item.title;
    document.getElementById("contentPlatform").value = item.platform;
    document.getElementById("contentDate").value = item.date;
    document.getElementById("contentViews").value = item.views;
    document.getElementById("contentLikes").value = item.likes;
    document.getElementById("contentComments").value = item.comments;
    document.getElementById("contentNotes").value = item.notes;
  } else {
    document.getElementById("contentFormTitle").textContent = "Tambah Konten";
  }
}

function closeContentForm() {
  document.getElementById("contentFormBox").classList.add("hidden");
}

document.getElementById("contentForm").addEventListener("submit", e => {
  e.preventDefault();

  const id = document.getElementById("contentId").value;

  const item = {
    id: id || uid("content"),
    title: document.getElementById("contentTitle").value,
    platform: document.getElementById("contentPlatform").value,
    date: document.getElementById("contentDate").value,
    views: Number(document.getElementById("contentViews").value),
    likes: Number(document.getElementById("contentLikes").value),
    comments: Number(document.getElementById("contentComments").value),
    notes: document.getElementById("contentNotes").value,
    status: id
      ? data.contents.find(x => x.id === id)?.status || "Ide"
      : "Ide"
  };

  const index = data.contents.findIndex(x => x.id === id);

  if (index >= 0) data.contents[index] = item;
  else data.contents.push(item);

  closeContentForm();
  saveData();
});

function renderPipeline() {
  const board = document.getElementById("pipelineBoard");

  board.innerHTML = statuses.map(status => {
    const items = data.contents.filter(x => x.status === status);

    return `
      <div class="pipeline-column"
        data-status="${status}"
        ondragover="event.preventDefault()"
        ondrop="dropContent(event, '${status}')">

        <h3>${status} · ${items.length}</h3>

        ${items.map(item => `
          <div class="content-card"
            draggable="true"
            ondragstart="dragContent(event, '${item.id}')">

            <strong>${escapeHTML(item.title)}</strong>
            <small>${escapeHTML(item.platform)}</small>
            ${item.date ? `<div class="badge">${item.date}</div>` : ""}

            <div class="actions">
              <button class="btn secondary" onclick="openContentForm('${item.id}')">Edit</button>
              <button class="btn danger" onclick="deleteContent('${item.id}')">Hapus</button>
            </div>
          </div>
        `).join("") || `<div class="empty">Belum ada konten</div>`}
      </div>
    `;
  }).join("");
}

function dragContent(event, id) {
  event.dataTransfer.setData("contentId", id);
}

function dropContent(event, status) {
  const id = event.dataTransfer.getData("contentId");
  const item = data.contents.find(x => x.id === id);

  if (item) {
    item.status = status;
    saveData();
  }
}

function deleteContent(id) {
  if (!confirm("Hapus konten ini?")) return;

  data.contents = data.contents.filter(x => x.id !== id);
  saveData();
}

function renderSchedule() {
  const date = document.getElementById("scheduleDate").value;
  const list = document.getElementById("scheduleList");

  const items = data.contents.filter(x => x.date && (!date || x.date === date));

  list.innerHTML = items.map(item => `
    <div class="card">
      <h3>${escapeHTML(item.title)}</h3>
      <p style="color:var(--muted)">${escapeHTML(item.platform)}</p>
      <span class="badge">${item.status}</span>
      <p>${item.date}</p>
      <button class="btn secondary" onclick="openContentForm('${item.id}');goToPage('pipeline')">Edit Konten</button>
    </div>
  `).join("") || `<div class="card empty">Tidak ada konten pada tanggal tersebut.</div>`;
}

function openRevenueForm(id = "") {
  document.getElementById("revenueFormBox").classList.remove("hidden");
  document.getElementById("revenueForm").reset();
  document.getElementById("revenueId").value = "";

  if (id) {
    const item = data.revenues.find(x => x.id === id);
    if (!item) return;

    document.getElementById("revenueId").value = item.id;
    document.getElementById("revenuePlatform").value = item.platform;
    document.getElementById("revenueAmount").value = item.amount;
    document.getElementById("revenueCurrency").value = item.currency;
    document.getElementById("revenueViews").value = item.views;
    document.getElementById("revenueDate").value = item.date;
    document.getElementById("revenueNotes").value = item.notes;
  }
}

function closeRevenueForm() {
  document.getElementById("revenueFormBox").classList.add("hidden");
}

document.getElementById("revenueForm").addEventListener("submit", e => {
  e.preventDefault();

  const id = document.getElementById("revenueId").value;

  const item = {
    id: id || uid("revenue"),
    platform: document.getElementById("revenuePlatform").value,
    amount: Number(document.getElementById("revenueAmount").value),
    currency: document.getElementById("revenueCurrency").value,
    views: Number(document.getElementById("revenueViews").value),
    date: document.getElementById("revenueDate").value,
    notes: document.getElementById("revenueNotes").value
  };

  const index = data.revenues.findIndex(x => x.id === id);

  if (index >= 0) data.revenues[index] = item;
  else data.revenues.push(item);

  closeRevenueForm();
  saveData();
});

function renderRevenue() {
  const tbody = document.getElementById("revenueTable");

  tbody.innerHTML = data.revenues.map(item => `
    <tr>
      <td>${escapeHTML(item.platform)}</td>
      <td>${money(item.amount, item.currency)}</td>
      <td>${number(item.views)}</td>
      <td>${item.date || "-"}</td>
      <td>
        <button class="btn secondary" onclick="openRevenueForm('${item.id}')">Edit</button>
        <button class="btn danger" onclick="deleteRevenue('${item.id}')">Hapus</button>
      </td>
    </tr>
  `).join("") || `
    <tr>
      <td colspan="5" class="empty">Belum ada data revenue.</td>
    </tr>
  `;
}

function deleteRevenue(id) {
  if (!confirm("Hapus data revenue ini?")) return;

  data.revenues = data.revenues.filter(x => x.id !== id);
  saveData();
}

function openDealForm(id = "") {
  document.getElementById("dealFormBox").classList.remove("hidden");
  document.getElementById("dealForm").reset();
  document.getElementById("dealId").value = "";

  if (id) {
    const item = data.deals.find(x => x.id === id);
    if (!item) return;

    document.getElementById("dealId").value = item.id;
    document.getElementById("dealBrand").value = item.brand;
    document.getElementById("dealCampaign").value = item.campaign;
    document.getElementById("dealValue").value = item.value;
    document.getElementById("dealCurrency").value = item.currency;
    document.getElementById("dealDeadline").value = item.deadline;
    document.getElementById("dealStatus").value = item.status;
    document.getElementById("dealPayment").value = item.payment;
    document.getElementById("dealNotes").value = item.notes;
  }
}

function closeDealForm() {
  document.getElementById("dealFormBox").classList.add("hidden");
}

document.getElementById("dealForm").addEventListener("submit", e => {
  e.preventDefault();

  const id = document.getElementById("dealId").value;

  const item = {
    id: id || uid("deal"),
    brand: document.getElementById("dealBrand").value,
    campaign: document.getElementById("dealCampaign").value,
    value: Number(document.getElementById("dealValue").value),
    currency: document.getElementById("dealCurrency").value,
    deadline: document.getElementById("dealDeadline").value,
    status: document.getElementById("dealStatus").value,
    payment: document.getElementById("dealPayment").value,
    notes: document.getElementById("dealNotes").value
  };

  const index = data.deals.findIndex(x => x.id === id);

  if (index >= 0) data.deals[index] = item;
  else data.deals.push(item);

  closeDealForm();
  saveData();
});

function renderDeals() {
  const list = document.getElementById("dealList");

  list.innerHTML = data.deals.map(item => `
    <div class="deal-card">
      <h3>${escapeHTML(item.brand)}</h3>
      <p>${escapeHTML(item.campaign)}</p>
      <h2>${money(item.value, item.currency)}</h2>
      <p>Deadline: ${item.deadline || "-"}</p>
      <span class="badge">${item.status}</span>
      <span class="badge">${item.payment}</span>
      <div class="actions">
        <button class="btn secondary" onclick="openDealForm('${item.id}')">Edit</button>
        <button class="btn danger" onclick="deleteDeal('${item.id}')">Hapus</button>
      </div>
    </div>
  `).join("") || `<div class="card empty">Belum ada brand deal.</div>`;
}

function deleteDeal(id) {
  if (!confirm("Hapus brand deal ini?")) return;

  data.deals = data.deals.filter(x => x.id !== id);
  saveData();
}

function openIdeaForm(id = "") {
  document.getElementById("ideaFormBox").classList.remove("hidden");
  document.getElementById("ideaForm").reset();
  document.getElementById("ideaId").value = "";

  if (id) {
    const item = data.ideas.find(x => x.id === id);
    if (!item) return;

    document.getElementById("ideaId").value = item.id;
    document.getElementById("ideaTitle").value = item.title;
    document.getElementById("ideaPlatform").value = item.platform;
    document.getElementById("ideaCategory").value = item.category;
    document.getElementById("ideaStatus").value = item.status;
    document.getElementById("ideaNotes").value = item.notes;
  }
}

function closeIdeaForm() {
  document.getElementById("ideaFormBox").classList.add("hidden");
}

document.getElementById("ideaForm").addEventListener("submit", e => {
  e.preventDefault();

  const id = document.getElementById("ideaId").value;

  const item = {
    id: id || uid("idea"),
    title: document.getElementById("ideaTitle").value,
    platform: document.getElementById("ideaPlatform").value,
    category: document.getElementById("ideaCategory").value,
    status: document.getElementById("ideaStatus").value,
    notes: document.getElementById("ideaNotes").value
  };

  const index = data.ideas.findIndex(x => x.id === id);

  if (index >= 0) data.ideas[index] = item;
  else data.ideas.push(item);

  closeIdeaForm();
  saveData();
});

function renderIdeas() {
  const list = document.getElementById("ideaList");

  list.innerHTML = data.ideas.map(item => `
    <div class="idea-card">
      <h3>${escapeHTML(item.title)}</h3>
      <p>${escapeHTML(item.category || "Tanpa kategori")}</p>
      <span class="badge">${item.platform}</span>
      <span class="badge">${item.status}</span>
      <p>${escapeHTML(item.notes)}</p>
      <div class="actions">
        <button class="btn secondary" onclick="openIdeaForm('${item.id}')">Edit</button>
        <button class="btn danger" onclick="deleteIdea('${item.id}')">Hapus</button>
      </div>
    </div>
  `).join("") || `<div class="card empty">Belum ada ide tersimpan.</div>`;
}

function deleteIdea(id) {
  if (!confirm("Hapus ide ini?")) return;

  data.ideas = data.ideas.filter(x => x.id !== id);
  saveData();
}

function externalSearch(platform) {
  const keyword = document.getElementById("ideaKeyword").value.trim();

  if (!keyword) {
    alert("Masukkan keyword terlebih dahulu.");
    return;
  }

  const urls = {
    google: "https://www.google.com/search?q=",
    youtube: "https://www.youtube.com/results?search_query=",
    tiktok: "https://www.tiktok.com/search?q=",
    pinterest: "https://www.pinterest.com/search/pins/?q="
  };

  window.open(urls[platform] + encodeURIComponent(keyword), "_blank");
}

function searchIdeas() {
  externalSearch("google");
}

function renderMedia() {
  const m = data.media;

  document.getElementById("mediaName").value = m.name;
  document.getElementById("mediaUsername").value = m.username;
  document.getElementById("mediaNiche").value = m.niche;
  document.getElementById("mediaBio").value = m.bio;
  document.getElementById("mediaFollowers").value = m.followers;
  document.getElementById("mediaEngagement").value = m.engagement;

  document.getElementById("mediaPreview").innerHTML = `
    <div class="media-name">${escapeHTML(m.username)}</div>
    <h1>${escapeHTML(m.name)}</h1>
    <h3>${escapeHTML(m.niche)}</h3>
    <p>${escapeHTML(m.bio)}</p>

    <div class="media-stat">
      <strong>${number(m.followers)}</strong><br>
      <small>Followers</small>
    </div>

    <div class="media-stat">
      <strong>${Number(m.engagement || 0).toFixed(1)}%</strong><br>
      <small>Engagement Rate</small>
    </div>

    <div class="media-stat">
      <strong>${number(data.contents.length)}</strong><br>
      <small>Total Content</small>
    </div>
  `;
}

document.getElementById("mediaForm").addEventListener("submit", e => {
  e.preventDefault();

  data.media = {
    name: document.getElementById("mediaName").value,
    username: document.getElementById("mediaUsername").value,
    niche: document.getElementById("mediaNiche").value,
    bio: document.getElementById("mediaBio").value,
    followers: Number(document.getElementById("mediaFollowers").value),
    engagement: Number(document.getElementById("mediaEngagement").value)
  };

  saveData();
});

function renderDashboard() {
  const totalViews = data.contents.reduce((sum, x) => sum + Number(x.views || 0), 0);
  const published = data.contents.filter(x => x.status === "Published").length;
  const totalRevenue = data.revenues
    .filter(x => x.currency === "IDR")
    .reduce((sum, x) => sum + Number(x.amount || 0), 0);

  document.getElementById("statContents").textContent = number(data.contents.length);
  document.getElementById("statPublished").textContent = number(published);
  document.getElementById("statViews").textContent = number(totalViews);
  document.getElementById("statRevenue").textContent = money(totalRevenue);

  document.getElementById("recentActivity").innerHTML =
    data.contents.slice(-5).reverse().map(item => `
      <div style="padding:12px 0;border-bottom:1px solid var(--border)">
        <strong>${escapeHTML(item.title)}</strong>
        <div style="color:var(--muted);font-size:13px">
          ${item.platform} · ${item.status}
        </div>
      </div>
    `).join("") || `<div class="empty">Belum ada aktivitas.</div>`;

  const contentLabels = statuses;
  const contentValues = statuses.map(status =>
    data.contents.filter(x => x.status === status).length
  );

  const revenueByPlatform = ["TikTok", "YouTube", "Facebook", "Instagram", "Brand Deal"]
    .map(platform =>
      data.revenues
        .filter(x => x.platform === platform && x.currency === "IDR")
        .reduce((sum, x) => sum + Number(x.amount || 0), 0)
    );

  if (charts.content) charts.content.destroy();
  if (charts.revenue) charts.revenue.destroy();

  charts.content = new Chart(document.getElementById("contentChart"), {
    type: "bar",
    data: {
      labels: contentLabels,
      datasets: [{
        label: "Jumlah Konten",
        data: contentValues
      }]
    },
    options: {
      responsive: true,
      plugins: {
        title: {
          display: true,
          text: "Distribusi Content Pipeline",
          color: "#ffffff"
        },
        legend: {
          labels: { color: "#ffffff" }
        }
      },
      scales: {
        x: { ticks: { color: "#aaa6c2" } },
        y: { ticks: { color: "#aaa6c2" } }
      }
    }
  });

  charts.revenue = new Chart(document.getElementById("revenueChart"), {
    type: "doughnut",
    data: {
      labels: ["TikTok", "YouTube", "Facebook", "Instagram", "Brand Deal"],
      datasets: [{
        label: "Revenue IDR",
        data: revenueByPlatform
      }]
    },
    options: {
      responsive: true,
      plugins: {
        title: {
          display: true,
          text: "Revenue per Platform",
          color: "#ffffff"
        },
        legend: {
          labels: { color: "#ffffff" }
        }
      }
    }
  });
}

function renderAll() {
  renderDashboard();
  renderPipeline();
  renderSchedule();
  renderRevenue();
  renderDeals();
  renderIdeas();
  renderMedia();
}

document.getElementById("scheduleDate").value =
  new Date().toISOString().slice(0, 10);

if (localStorage.getItem(LOGIN_KEY)) {
  showApp();
}
</script>

</body>
</html>
